# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
.eggs/

# Build artifacts
build/
dist/
site/
*.whl
*.tar.gz

# Virtual environments
.venv/
venv/
env/
.python-version

# Tool caches
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/
.cache/

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS junk
.DS_Store
Thumbs.db

# Local project state
.env
.env.*
!.env.example
state.json
cache/
*.log
data/rerank_train*.jsonl

# ─── Dataset Pipeline ─────────────────────────────────────────────────────────
# data/collab/*.jsonl — anonymized contributor files, OK to commit (output of share_my_canvas.py)
data/rerank_*.jsonl
rerank_*.jsonl
# Reranker subdir data (same rules — local only)
reranker/data/*.jsonl
reranker/data/collab/
reranker/outputs/
outputs/
*.gguf
*.safetensors

# Model checkpoints
checkpoints/
adapter_model.safetensors

# Local Spark data (contains private API tokens)
spark-maker-data/

# AI / GSD workflow artifacts (local only, not app code)
.planning/
.claude/
CLAUDE.md
scripts/.claude/
scripts/claude_gsd_cron.sh
data/trajectories/
.opencode/

# Extension build artifacts
extension/node_modules/
extension/coverage/
popup-visual-check.png
popup-grades-tab.png
.playwright-mcp/


# Local QA / development screenshots (not part of the source)
audit-*.png
popup-after-overhaul.png
popup-grades-view.png
popup-no-token.png
docs-site/screenshots/
