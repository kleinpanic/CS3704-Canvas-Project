# Trajectories (Legacy Dataset)

This directory held v2 calendar-agent training trajectories. The collection script
(`scripts/collect_trajectories.py`) was Spark-internal and depended on a private
inference endpoint. It has not been restored for the public repo.

`data/trajectories/collab/` is a closed legacy dataset. New trajectory contributions
are not accepted at this time.

For Canvas data contributions (courses, assignments, todo items), see
[docs/contributing-data.md](../../docs/contributing-data.md).
